import XCTest
@testable import CustomView_Library

final class CustomView_LibraryTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CustomView_Library().text, "Hello, World!")
    }
}
